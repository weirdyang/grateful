import { createStore } from 'vuex'
import usersModule from './modules/users'

export default createStore({
  modules: {
    users: usersModule,
  },
  state: {
  },
  mutations: {
  },
  actions: {
  }
})
